---
import "tw-base"
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import MiniFish from '../components/MiniFish.astro';
import HeaderIcon from '../components/HeaderIcon.astro';
import Dropdown from '../components/Dropdown.astro';



---

<Layout title="Welcome to Astro.">
  <main>
    <header class="flex justify-between max-h-min">
      <div class="invisible w-0 my-auto sm:tracking-tightest sm:w-fit sm:visible"><MiniFish /></div>
      <div class="my-auto mr-auto text-sm sm:ml-auto text-nowrap">
        <span class="hidden sm:visible">  welcome to the </span>
        <ins class="float-left text-blue-300 sm:float-none">fish-lsp</ins>
      </div>
      <div class="hidden sm:inline-flex sm:gap-5">
        <HeaderIcon name="github" />
        <HeaderIcon name="npm" />
      </div>
      <div class="visible sm:hidden">
        <Dropdown />
      </div>
    </header>
    <div class="pt-2 pb-4">
      <img class="" src="/help-msg.svg" alt="Astro logo" />
    </div>

    <p id="description">
      <strong class="text-gradient">fish-lsp</strong> is a <a href="https://lsif.dev/">language server protocol (LSP)</a>
      implementation for the fish shell. It aims to provide a rich set of features for the <a href="https://fishshell.com/">fish shell</a>,
      including <i>auto-completion</i>, <i>hover</i>, <i>go-to references</i>, and <i>many others</i>.
    </p>
    <br />

    <ul role="list" class="link-card-grid">
      <Card
        href="https://github.com/ndonfris/fish-lsp/"
        title="github repo"
        body="source code for the project."
      />
      <Card
        href="https://www.npmjs.com/package/fish-lsp/"
        title="npm package"
        body="release avaliable on node package manager."
      />
      <Card
        href="https://github.com/ndonfris/fish-lsp-language-clients"
        title="Integrations"
        body="vscode, vim, neovim, helix, and more."
      />
      <Card
        href="https://github.com/ndonfris/fish-lsp/discussions/"
        title="Discussions"
        body="Come say hi ❤️"
      />
    </ul>
  </main>
</Layout>

<style>
main {
  margin: auto;
  padding: 1rem;
  width: 800px;
  max-width: calc(100% - 2rem);
  color: white;
  font-size: 20px;
  line-height: 1.6;
}
.astro-a {
  position: absolute;
  top: -32px;
  left: 50%;
  transform: translatex(-50%);
  width: 220px;
  height: auto;
  z-index: -1;
}
h1 {
  font-size: 4rem;
  font-weight: 700;
  line-height: 1;
  text-align: center;
  margin-bottom: 1em;
}
.text-gradient {
  background-image: var(--accent-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-size: 400%;
  background-position: 0%;
}
.instructions {
  margin-bottom: 2rem;
  border: 1px solid rgba(var(--accent-light), 25%);
  background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
  padding: 1.5rem;
  border-radius: 8px;
}
.instructions code {
  font-size: 0.8em;
  font-weight: bold;
  background: rgba(var(--accent-light), 12%);
  color: rgb(var(--accent-light));
  border-radius: 4px;
  padding: 0.3em 0.4em;
}
.instructions strong {
  color: rgb(var(--accent-light));
}
.link-card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
  gap: 2rem;
  padding: 0;
}
#description a {
  color: rgba(156,92, 238, 0.9);
  text-decoration: underline;
}

#description i {
  color: rgb(184 138 243);
  text-decoration: underline;
}
</style>
